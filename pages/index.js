import Heading from "../components/Heading"
import styles from "../styles/Home.module.scss"

const Home = () => (
  <div className={styles.wrapper}>
    <Heading text="Got the life"/>
  </div>
)

export default Home